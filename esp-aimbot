--// Advanced Debug Menu v5.0 - Chameleon Edition
--// PlaceId: 110808833601416
--// NEW v5: ESP count hiện rõ | Lines từ trên | Auto-ESP theo team | FOV % slider

local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer
local Camera      = workspace.CurrentCamera

--==================================================
-- CẤU HÌNH
--==================================================

local CONFIG = {
    ESP_COLOR_ENEMY         = Color3.fromRGB(255, 60, 60),
    ESP_COLOR_ALLY          = Color3.fromRGB(60, 180, 255),
    ESP_FILL_TRANSPARENCY   = 0.60,
    ESP_MAX_DISTANCE        = 600,
    NAME_TAG_SIZE           = 14,
    AIM_FOV_MAX             = 320,   -- pixel tối đa (100%)
    AIM_FOV_PCT             = 50,    -- % mặc định → 160px
    AIM_SMOOTH              = 0.10,
    AIM_TARGET_PART         = "HumanoidRootPart",
    AIM_SHOW_FOV_CIRCLE     = true,
    AIM_FOV_COLOR           = Color3.fromRGB(255, 255, 255),
}

-- FOV thực tế (px) tính từ %
local function getFOVpx()
    return math.floor(CONFIG.AIM_FOV_MAX * CONFIG.AIM_FOV_PCT / 100)
end

--==================================================
-- DETECT TEAM - CHAMELEON SPECIFIC
--==================================================

local function scanChameleonRole(player)
    local pg = player:FindFirstChild("PlayerGui")
    if pg then
        local function deepSearch(parent, depth)
            if depth > 6 then return nil end
            for _, child in ipairs(parent:GetChildren()) do
                if child:IsA("TextLabel") or child:IsA("TextButton") then
                    local t = child.Text
                    if t == "H" or t == "Hider" then return "Hider" end
                    if t == "S" or t == "Seeker" then return "Seeker" end
                end
                local found = deepSearch(child, depth + 1)
                if found then return found end
            end
            return nil
        end
        local role = deepSearch(pg, 0)
        if role then return role end
    end

    local ls = player:FindFirstChild("leaderstats")
    if ls then
        local roleVal = ls:FindFirstChild("Role") or ls:FindFirstChild("Team")
            or ls:FindFirstChild("Side") or ls:FindFirstChild("Job")
        if roleVal then
            local v = tostring(roleVal.Value)
            if v:lower():find("hider") or v == "H" then return "Hider" end
            if v:lower():find("seeker") or v == "S" then return "Seeker" end
        end
    end

    local attrs = {"Role", "Team", "Side", "PlayerRole", "GameRole"}
    for _, attr in ipairs(attrs) do
        local val = player:GetAttribute(attr)
        if val then
            local s = tostring(val):lower()
            if s:find("hid") or s == "h" then return "Hider" end
            if s:find("seek") or s == "s" then return "Seeker" end
        end
        local char = player.Character
        if char then
            local val2 = char:GetAttribute(attr)
            if val2 then
                local s = tostring(val2):lower()
                if s:find("hid") or s == "h" then return "Hider" end
                if s:find("seek") or s == "s" then return "Seeker" end
            end
        end
    end

    local team = player.Team
    if team then
        local tn = team.Name:lower()
        if tn:find("hid") then return "Hider" end
        if tn:find("seek") then return "Seeker" end
    end

    return nil
end

local function getRole(player) return scanChameleonRole(player) end
local function getMyRole()     return getRole(LocalPlayer) end

local function isEnemy(player)
    if player == LocalPlayer then return false end
    local myRole    = getMyRole()
    local theirRole = getRole(player)
    if myRole and theirRole then return myRole ~= theirRole end
    if LocalPlayer.Team and player.Team then
        return LocalPlayer.Team ~= player.Team
    end
    return true
end

--==================================================
-- TRẠNG THÁI
--==================================================

local ESP_ENABLED  = false
local AIM_ENABLED  = false
local MENU_VISIBLE = true
local espObjects   = {}
local espCount     = 0
local lastMyRole   = nil   -- theo dõi đổi team → auto re-ESP

-- Sub-options checkbox
local OPT = {
    name     = true,
    dist     = true,
    box      = true,
    skeleton = true,
    lines    = false,
    fov      = true,   -- NEW: bật/tắt vòng FOV
}

--==================================================
-- TIỆN ÍCH
--==================================================

local function getDistanceTo(player)
    local mc = LocalPlayer.Character
    if not mc then return math.huge end
    local mr = mc:FindFirstChild("HumanoidRootPart")
    local tc = player.Character
    if not tc then return math.huge end
    local tr = tc:FindFirstChild("HumanoidRootPart")
    if not mr or not tr then return math.huge end
    return (tr.Position - mr.Position).Magnitude
end

local function worldToVP(pos)
    local sp, on = Camera:WorldToViewportPoint(pos)
    return Vector2.new(sp.X, sp.Y), on
end

--==================================================
-- GUI
--==================================================

local oldGui = LocalPlayer.PlayerGui:FindFirstChild("ChameleonDebug")
if oldGui then oldGui:Destroy() end

local gui = Instance.new("ScreenGui")
gui.Name             = "ChameleonDebug"
gui.ResetOnSpawn     = false
gui.ZIndexBehavior   = Enum.ZIndexBehavior.Sibling
gui.IgnoreGuiInset   = true
gui.Parent           = LocalPlayer:WaitForChild("PlayerGui")

-- Menu: rộng 370, cao hơn v4 (thêm slider FOV)
local MENU_W = 370
local MENU_H = 340

local main = Instance.new("Frame", gui)
main.Name             = "Main"
main.Size             = UDim2.fromOffset(MENU_W, MENU_H)
main.Position         = UDim2.new(0.5, -MENU_W/2, 0.5, -MENU_H/2)
main.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
main.BorderSizePixel  = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 16)
local mainStroke = Instance.new("UIStroke", main)
mainStroke.Color     = Color3.fromRGB(70, 70, 110)
mainStroke.Thickness = 1.5

-- Header
local header = Instance.new("Frame", main)
header.Size             = UDim2.new(1, 0, 0, 46)
header.BackgroundColor3 = Color3.fromRGB(28, 28, 45)
header.BorderSizePixel  = 0
Instance.new("UICorner", header).CornerRadius = UDim.new(0, 16)
local hbFix = Instance.new("Frame", header)
hbFix.Size             = UDim2.new(1, 0, 0.5, 0)
hbFix.Position         = UDim2.new(0, 0, 0.5, 0)
hbFix.BackgroundColor3 = Color3.fromRGB(28, 28, 45)
hbFix.BorderSizePixel  = 0

local titleLbl = Instance.new("TextLabel", header)
titleLbl.Size             = UDim2.new(1, -50, 1, 0)
titleLbl.Position         = UDim2.fromOffset(14, 0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text             = "🎨 Chameleon ESP v5"
titleLbl.TextColor3       = Color3.new(1, 1, 1)
titleLbl.TextSize         = 15
titleLbl.Font             = Enum.Font.GothamBold
titleLbl.TextXAlignment   = Enum.TextXAlignment.Left

local collapseBtn = Instance.new("TextButton", header)
collapseBtn.Size             = UDim2.fromOffset(28, 28)
collapseBtn.Position         = UDim2.new(1, -36, 0, 9)
collapseBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 80)
collapseBtn.Text             = "–"
collapseBtn.TextColor3       = Color3.new(1, 1, 1)
collapseBtn.TextSize         = 18
collapseBtn.Font             = Enum.Font.GothamBold
collapseBtn.AutoButtonColor  = false
Instance.new("UICorner", collapseBtn).CornerRadius = UDim.new(0, 8)

-- Content
local content = Instance.new("Frame", main)
content.Name                   = "Content"
content.Size                   = UDim2.new(1, 0, 1, -46)
content.Position               = UDim2.fromOffset(0, 46)
content.BackgroundTransparency = 1

--==================================================
-- INFO BAR: ESP count + Role  (hàng ngang)
-- NEW: countLbl hiện số người rõ hơn + màu xanh lá
--==================================================

-- Nền badge ESP count
local countBadge = Instance.new("Frame", content)
countBadge.Size             = UDim2.fromOffset(130, 22)
countBadge.Position         = UDim2.fromOffset(14, 6)
countBadge.BackgroundColor3 = Color3.fromRGB(30, 60, 35)
countBadge.BorderSizePixel  = 0
Instance.new("UICorner", countBadge).CornerRadius = UDim.new(0, 6)

local countLbl = Instance.new("TextLabel", countBadge)
countLbl.Size             = UDim2.fromScale(1, 1)
countLbl.BackgroundTransparency = 1
countLbl.Text             = "👁  Thấy: 0 người"
countLbl.TextColor3       = Color3.fromRGB(80, 220, 100)
countLbl.TextSize         = 11
countLbl.Font             = Enum.Font.GothamBold
countLbl.TextXAlignment   = Enum.TextXAlignment.Center

local roleLbl = Instance.new("TextLabel", content)
roleLbl.Size             = UDim2.new(1, -158, 0, 22)
roleLbl.Position         = UDim2.fromOffset(152, 6)
roleLbl.BackgroundTransparency = 1
roleLbl.Text             = "❓ Đang xác định..."
roleLbl.TextColor3       = Color3.fromRGB(255, 220, 80)
roleLbl.TextSize         = 11
roleLbl.Font             = Enum.Font.GothamBold
roleLbl.TextXAlignment   = Enum.TextXAlignment.Right

--==================================================
-- TOGGLE FACTORY
--==================================================

local function makeToggle(parent, label, xPos, yPos, width, getState, setState)
    local btn = Instance.new("TextButton", parent)
    btn.Size             = UDim2.fromOffset(width, 44)
    btn.Position         = UDim2.fromOffset(xPos, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 52)
    btn.AutoButtonColor  = false
    btn.BorderSizePixel  = 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)

    local lbl2 = Instance.new("TextLabel", btn)
    lbl2.Size             = UDim2.new(1, -56, 1, 0)
    lbl2.Position         = UDim2.fromOffset(10, 0)
    lbl2.BackgroundTransparency = 1
    lbl2.Text             = label
    lbl2.TextColor3       = Color3.new(1, 1, 1)
    lbl2.TextSize         = 13
    lbl2.Font             = Enum.Font.GothamBold
    lbl2.TextXAlignment   = Enum.TextXAlignment.Left

    local pill = Instance.new("Frame", btn)
    pill.Size             = UDim2.fromOffset(40, 22)
    pill.Position         = UDim2.new(1, -48, 0.5, -11)
    pill.BackgroundColor3 = Color3.fromRGB(70, 70, 95)
    pill.BorderSizePixel  = 0
    Instance.new("UICorner", pill).CornerRadius = UDim.new(0.5, 0)

    local dot = Instance.new("Frame", pill)
    dot.Size             = UDim2.fromOffset(16, 16)
    dot.Position         = UDim2.fromOffset(3, 3)
    dot.BackgroundColor3 = Color3.new(1, 1, 1)
    dot.BorderSizePixel  = 0
    Instance.new("UICorner", dot).CornerRadius = UDim.new(0.5, 0)

    local function refreshVisual()
        local on = getState()
        local ti = TweenInfo.new(0.15)
        TweenService:Create(pill, ti, {
            BackgroundColor3 = on and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(70, 70, 95)
        }):Play()
        TweenService:Create(dot, ti, {
            Position = on and UDim2.fromOffset(21, 3) or UDim2.fromOffset(3, 3)
        }):Play()
    end

    btn.Activated:Connect(function()
        setState(not getState())
        refreshVisual()
    end)

    return btn, refreshVisual
end

--==================================================
-- TOGGLE ESP + AIMBOT (2 cột)
--==================================================

local HALF_W = (MENU_W - 42) / 2

local espBtnRef, refreshESPVisual = makeToggle(
    content, "🔍  ESP", 14, 35, HALF_W,
    function() return ESP_ENABLED end,
    function(v) ESP_ENABLED = v end
)

local aimBtnRef, refreshAIMVisual = makeToggle(
    content, "🎯  Aimbot", 14 + HALF_W + 8, 35, HALF_W,
    function() return AIM_ENABLED end,
    function(v) AIM_ENABLED = v end
)

-- Separator
local function sep(y)
    local f = Instance.new("Frame", content)
    f.Size             = UDim2.new(1, -28, 0, 1)
    f.Position         = UDim2.fromOffset(14, y)
    f.BackgroundColor3 = Color3.fromRGB(45, 45, 68)
    f.BorderSizePixel  = 0
end

sep(87)

--==================================================
-- CHECKBOX FACTORY
--==================================================

local function makeCheck(parent, label, key, xPos, yPos, width, optTable)
    local btn = Instance.new("TextButton", parent)
    btn.Size             = UDim2.fromOffset(width, 30)
    btn.Position         = UDim2.fromOffset(xPos, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(22, 22, 34)
    btn.AutoButtonColor  = false
    btn.BorderSizePixel  = 0
    btn.Text             = ""
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

    local box = Instance.new("Frame", btn)
    box.Size             = UDim2.fromOffset(20, 20)
    box.Position         = UDim2.fromOffset(7, 5)
    box.BackgroundColor3 = optTable[key] and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(50, 50, 72)
    box.BorderSizePixel  = 0
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)

    local tick = Instance.new("TextLabel", box)
    tick.Size             = UDim2.fromScale(1, 1)
    tick.BackgroundTransparency = 1
    tick.Text             = optTable[key] and "✓" or ""
    tick.TextColor3       = Color3.new(1, 1, 1)
    tick.TextSize         = 13
    tick.Font             = Enum.Font.GothamBold
    tick.TextXAlignment   = Enum.TextXAlignment.Center
    tick.TextYAlignment   = Enum.TextYAlignment.Center

    local lbl3 = Instance.new("TextLabel", btn)
    lbl3.Size             = UDim2.new(1, -34, 1, 0)
    lbl3.Position         = UDim2.fromOffset(32, 0)
    lbl3.BackgroundTransparency = 1
    lbl3.Text             = label
    lbl3.TextColor3       = Color3.fromRGB(200, 200, 220)
    lbl3.TextSize         = 11
    lbl3.Font             = Enum.Font.Gotham
    lbl3.TextXAlignment   = Enum.TextXAlignment.Left
    lbl3.TextTruncate     = Enum.TextTruncate.AtEnd

    btn.Activated:Connect(function()
        optTable[key] = not optTable[key]
        box.BackgroundColor3 = optTable[key] and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(50, 50, 72)
        tick.Text = optTable[key] and "✓" or ""
    end)

    return btn
end

--==================================================
-- SECTION: TÙY CHỈNH ESP
--==================================================

local espSectionLbl = Instance.new("TextLabel", content)
espSectionLbl.Size             = UDim2.new(1, -28, 0, 14)
espSectionLbl.Position         = UDim2.fromOffset(14, 93)
espSectionLbl.BackgroundTransparency = 1
espSectionLbl.Text             = "▸ ESP"
espSectionLbl.TextColor3       = Color3.fromRGB(130, 130, 165)
espSectionLbl.TextSize         = 10
espSectionLbl.Font             = Enum.Font.GothamBold
espSectionLbl.TextXAlignment   = Enum.TextXAlignment.Left

local COL1_X = 14
local COL2_X = 14 + HALF_W + 8
local CHECK_W = HALF_W

-- Hàng 1
makeCheck(content, "Tên nhân vật",      "name",     COL1_X, 110, CHECK_W, OPT)
makeCheck(content, "Khoảng cách",       "dist",     COL2_X, 110, CHECK_W, OPT)
-- Hàng 2
makeCheck(content, "Khung (Box)",        "box",      COL1_X, 144, CHECK_W, OPT)
makeCheck(content, "Xương (Skeleton)",   "skeleton", COL2_X, 144, CHECK_W, OPT)
-- Hàng 3 (toàn chiều rộng)
makeCheck(content, "Kẻ chỉ (Lines)",     "lines",    COL1_X, 178, CHECK_W, OPT)

sep(214)

--==================================================
-- SECTION: TÙY CHỈNH AIMBOT + FOV
-- NEW: checkbox FOV on/off + slider % FOV
--==================================================

local aimSectionLbl = Instance.new("TextLabel", content)
aimSectionLbl.Size             = UDim2.new(1, -28, 0, 14)
aimSectionLbl.Position         = UDim2.fromOffset(14, 220)
aimSectionLbl.BackgroundTransparency = 1
aimSectionLbl.Text             = "▸ Aimbot"
aimSectionLbl.TextColor3       = Color3.fromRGB(130, 130, 165)
aimSectionLbl.TextSize         = 10
aimSectionLbl.Font             = Enum.Font.GothamBold
aimSectionLbl.TextXAlignment   = Enum.TextXAlignment.Left

-- Checkbox bật/tắt vòng FOV
makeCheck(content, "Hiện vòng FOV", "fov", COL1_X, 237, CHECK_W, OPT)

-- Label FOV %
local fovLabel = Instance.new("TextLabel", content)
fovLabel.Size             = UDim2.fromOffset(90, 22)
fovLabel.Position         = UDim2.fromOffset(COL2_X, 237)
fovLabel.BackgroundTransparency = 1
fovLabel.Text             = string.format("FOV: %d%%", CONFIG.AIM_FOV_PCT)
fovLabel.TextColor3       = Color3.fromRGB(200, 200, 220)
fovLabel.TextSize         = 11
fovLabel.Font             = Enum.Font.GothamBold
fovLabel.TextXAlignment   = Enum.TextXAlignment.Left

-- Slider track
local SLIDER_X  = COL2_X
local SLIDER_Y  = 261
local SLIDER_W  = CHECK_W - 4
local SLIDER_H  = 16

local sliderTrack = Instance.new("TextButton", content)
sliderTrack.Size             = UDim2.fromOffset(SLIDER_W, SLIDER_H)
sliderTrack.Position         = UDim2.fromOffset(SLIDER_X, SLIDER_Y)
sliderTrack.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
sliderTrack.BorderSizePixel  = 0
sliderTrack.Text             = ""
sliderTrack.AutoButtonColor  = false
Instance.new("UICorner", sliderTrack).CornerRadius = UDim.new(0.5, 0)

local sliderFill = Instance.new("Frame", sliderTrack)
sliderFill.Size             = UDim2.new(CONFIG.AIM_FOV_PCT / 100, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(80, 140, 255)
sliderFill.BorderSizePixel  = 0
Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(0.5, 0)

local sliderThumb = Instance.new("Frame", sliderTrack)
sliderThumb.Size             = UDim2.fromOffset(20, 20)
sliderThumb.AnchorPoint      = Vector2.new(0.5, 0.5)
sliderThumb.Position         = UDim2.new(CONFIG.AIM_FOV_PCT / 100, 0, 0.5, 0)
sliderThumb.BackgroundColor3 = Color3.new(1, 1, 1)
sliderThumb.BorderSizePixel  = 0
Instance.new("UICorner", sliderThumb).CornerRadius = UDim.new(0.5, 0)

-- FOV circle frame (ở đây khai báo để updateFOVCircle dùng được)
local fovFrame = Instance.new("Frame", gui)
fovFrame.BackgroundTransparency = 1
fovFrame.BorderSizePixel        = 0
fovFrame.Visible                = false
Instance.new("UICorner", fovFrame).CornerRadius = UDim.new(0.5, 0)
local fovStroke = Instance.new("UIStroke", fovFrame)
fovStroke.Color     = CONFIG.AIM_FOV_COLOR
fovStroke.Thickness = 1.5

local function updateFOVCircle()
    local px = getFOVpx()
    fovFrame.Size     = UDim2.fromOffset(px * 2, px * 2)
    fovFrame.Position = UDim2.new(0.5, -px, 0.5, -px)
end
updateFOVCircle()

-- Slider interaction
local sliderDragging = false

local function setFOVPct(pct)
    pct = math.clamp(math.floor(pct), 1, 100)
    CONFIG.AIM_FOV_PCT = pct
    local t = pct / 100
    sliderFill.Size     = UDim2.new(t, 0, 1, 0)
    sliderThumb.Position = UDim2.new(t, 0, 0.5, 0)
    fovLabel.Text = string.format("FOV: %d%%", pct)
    updateFOVCircle()
end

local function sliderFromInput(input)
    local trackPos  = sliderTrack.AbsolutePosition
    local trackSize = sliderTrack.AbsoluteSize
    local relX = input.Position.X - trackPos.X
    local pct  = (relX / trackSize.X) * 100
    setFOVPct(pct)
end

sliderTrack.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        sliderDragging = true
        sliderFromInput(input)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if not sliderDragging then return end
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        sliderFromInput(input)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        sliderDragging = false
    end
end)

-- + / - buttons cho slider (dễ dùng trên mobile)
local function makeNudgeBtn(label, dx, xOff)
    local btn = Instance.new("TextButton", content)
    btn.Size             = UDim2.fromOffset(28, 22)
    btn.Position         = UDim2.fromOffset(SLIDER_X + SLIDER_W + xOff, SLIDER_Y - 3)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 68)
    btn.Text             = label
    btn.TextColor3       = Color3.new(1, 1, 1)
    btn.TextSize         = 14
    btn.Font             = Enum.Font.GothamBold
    btn.AutoButtonColor  = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    btn.Activated:Connect(function()
        setFOVPct(CONFIG.AIM_FOV_PCT + dx)
    end)
end
makeNudgeBtn("–", -5,  4)
makeNudgeBtn("+",  5, 36)

--==================================================
-- ESP OBJECTS (xương + highlight + billboard)
--==================================================

local BONES = {
    {"Head","UpperTorso"},
    {"UpperTorso","LowerTorso"},
    {"UpperTorso","LeftUpperArm"},
    {"LeftUpperArm","LeftLowerArm"},
    {"LeftLowerArm","LeftHand"},
    {"UpperTorso","RightUpperArm"},
    {"RightUpperArm","RightLowerArm"},
    {"RightLowerArm","RightHand"},
    {"LowerTorso","LeftUpperLeg"},
    {"LeftUpperLeg","LeftLowerLeg"},
    {"LeftLowerLeg","LeftFoot"},
    {"LowerTorso","RightUpperLeg"},
    {"RightUpperLeg","RightLowerLeg"},
    {"RightLowerLeg","RightFoot"},
}

local function makeLine(color)
    local f = Instance.new("Frame", gui)
    f.BackgroundColor3 = color or Color3.new(1,1,1)
    f.BorderSizePixel  = 0
    f.AnchorPoint      = Vector2.new(0.5, 0.5)
    f.Visible          = false
    return f
end

local function drawLine(f, from2D, to2D, thick)
    local d = to2D - from2D
    local len = d.Magnitude
    if len < 1 then f.Visible = false; return end
    f.Size     = UDim2.fromOffset(len, thick or 1.5)
    f.Position = UDim2.fromOffset((from2D.X+to2D.X)/2, (from2D.Y+to2D.Y)/2)
    f.Rotation = math.deg(math.atan2(d.Y, d.X))
    f.Visible  = true
end

local function removeESP(player)
    local obj = espObjects[player]
    if not obj then return end
    if obj.hl      then obj.hl:Destroy() end
    if obj.bbg     then obj.bbg:Destroy() end
    if obj.espLine then obj.espLine:Destroy() end
    for _, b in ipairs(obj.bones or {}) do b:Destroy() end
    espObjects[player] = nil
end

local function addESP(player)
    removeESP(player)
    local char = player.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local col = CONFIG.ESP_COLOR_ENEMY

    local hl = Instance.new("Highlight")
    hl.Name                = "ESP_HL"
    hl.Adornee             = char
    hl.FillColor           = col
    hl.OutlineColor        = col
    hl.FillTransparency    = CONFIG.ESP_FILL_TRANSPARENCY
    hl.OutlineTransparency = 0
    hl.DepthMode           = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Parent              = char

    local bbg = Instance.new("BillboardGui", gui)
    bbg.Adornee     = root
    bbg.Size        = UDim2.fromOffset(190, 50)
    bbg.StudsOffset = Vector3.new(0, 3.2, 0)
    bbg.AlwaysOnTop = true

    local nameLbl = Instance.new("TextLabel", bbg)
    nameLbl.Size                   = UDim2.new(1, 0, 0.54, 0)
    nameLbl.BackgroundTransparency = 1
    nameLbl.Text                   = player.DisplayName ~= "" and player.DisplayName or player.Name
    nameLbl.TextColor3             = col
    nameLbl.TextStrokeTransparency = 0.4
    nameLbl.TextStrokeColor3       = Color3.new(0,0,0)
    nameLbl.TextSize               = CONFIG.NAME_TAG_SIZE + 2
    nameLbl.Font                   = Enum.Font.GothamBold

    local distLbl = Instance.new("TextLabel", bbg)
    distLbl.Size                   = UDim2.new(1, 0, 0.46, 0)
    distLbl.Position               = UDim2.new(0, 0, 0.54, 0)
    distLbl.BackgroundTransparency = 1
    distLbl.Text                   = ""
    distLbl.TextColor3             = Color3.fromRGB(255, 240, 160)
    distLbl.TextStrokeTransparency = 0.4
    distLbl.TextStrokeColor3       = Color3.new(0,0,0)
    distLbl.TextSize               = CONFIG.NAME_TAG_SIZE
    distLbl.Font                   = Enum.Font.Gotham

    local espLine = makeLine(col)

    local bones = {}
    for _ = 1, #BONES do
        table.insert(bones, makeLine(Color3.fromRGB(255, 255, 255)))
    end

    espObjects[player] = {
        hl      = hl,
        bbg     = bbg,
        nameLbl = nameLbl,
        distLbl = distLbl,
        espLine = espLine,
        bones   = bones,
        char    = char,
    }
end

--==================================================
-- AUTO ESP KHI ĐỔI TEAM
-- Khi phát hiện myRole thay đổi (H↔S), tự rebuild toàn bộ ESP
--==================================================

local function rebuildAllESP()
    for _, p in ipairs(Players:GetPlayers()) do removeESP(p) end
    espCount = 0
    if not ESP_ENABLED then return end
    task.wait(0.2)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and isEnemy(p) then addESP(p) end
    end
end

--==================================================
-- UPDATE PER FRAME
--==================================================

local vp = Camera.ViewportSize

RunService.RenderStepped:Connect(function()
    vp = Camera.ViewportSize

    -- NEW: đường kẻ chỉ từ TOP-CENTER màn hình
    local lineOrigin = Vector2.new(vp.X / 2, 0)

    espCount = 0

    -- Hiện/ẩn vòng FOV
    fovFrame.Visible = AIM_ENABLED and OPT.fov

    -- Cập nhật role label
    local myRole = getMyRole()
    if myRole == "Seeker" then
        roleLbl.Text       = "🔎 Người Đi Tìm (S)"
        roleLbl.TextColor3 = Color3.fromRGB(255, 100, 100)
    elseif myRole == "Hider" then
        roleLbl.Text       = "🫣 Người Đi Trốn (H)"
        roleLbl.TextColor3 = Color3.fromRGB(100, 200, 255)
    else
        roleLbl.Text       = "❓ Đang xác định..."
        roleLbl.TextColor3 = Color3.fromRGB(180, 180, 100)
    end

    -- AUTO ESP khi đổi team
    if myRole ~= lastMyRole then
        lastMyRole = myRole
        if ESP_ENABLED then
            rebuildAllESP()
        end
    end

    -- Update ESP mỗi frame
    for _, player in ipairs(Players:GetPlayers()) do
        if player == LocalPlayer then continue end

        local obj = espObjects[player]

        if obj and obj.char ~= player.Character then
            removeESP(player)
            obj = nil
        end

        if not ESP_ENABLED or not isEnemy(player) then
            if obj then
                if obj.hl      then obj.hl.Enabled = false end
                if obj.bbg     then obj.bbg.Enabled = false end
                if obj.espLine then obj.espLine.Visible = false end
                for _, b in ipairs(obj.bones or {}) do b.Visible = false end
            end
            continue
        end

        local char = player.Character
        if not char then continue end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then continue end

        local dist = getDistanceTo(player)
        if dist > CONFIG.ESP_MAX_DISTANCE then
            if obj then
                if obj.hl      then obj.hl.Enabled  = false end
                if obj.bbg     then obj.bbg.Enabled  = false end
                if obj.espLine then obj.espLine.Visible = false end
                for _, b in ipairs(obj.bones or {}) do b.Visible = false end
            end
            continue
        end

        espCount = espCount + 1

        if not obj then
            addESP(player)
            obj = espObjects[player]
        end
        if not obj then continue end

        local showBox  = OPT.box
        local showName = OPT.name
        local showDist = OPT.dist
        local showSkel = OPT.skeleton
        local showLine = OPT.lines

        if obj.hl then obj.hl.Enabled = showBox end

        if obj.bbg then
            obj.bbg.Enabled = showName or showDist
            if obj.nameLbl then obj.nameLbl.Visible = showName end
            if obj.distLbl then
                obj.distLbl.Visible = showDist
                if showDist then
                    obj.distLbl.Text = string.format("📏 %.0f m", dist)
                end
            end
        end

        -- NEW: đường kẻ từ TOP-CENTER
        if obj.espLine then
            if showLine then
                local rp2d, on = worldToVP(root.Position)
                if on then
                    drawLine(obj.espLine, lineOrigin, rp2d, 1.5)
                else
                    obj.espLine.Visible = false
                end
            else
                obj.espLine.Visible = false
            end
        end

        for i, bonePair in ipairs(BONES) do
            local bLine = obj.bones[i]
            if not bLine then continue end
            if showSkel then
                local p1 = char:FindFirstChild(bonePair[1])
                local p2 = char:FindFirstChild(bonePair[2])
                if p1 and p2 then
                    local a2d, on1 = worldToVP(p1.Position)
                    local b2d, on2 = worldToVP(p2.Position)
                    if on1 or on2 then
                        drawLine(bLine, a2d, b2d, 1.2)
                    else
                        bLine.Visible = false
                    end
                else
                    bLine.Visible = false
                end
            else
                bLine.Visible = false
            end
        end
    end

    -- Update badge count
    if espCount > 0 then
        countLbl.Text             = string.format("👁  Thấy: %d người", espCount)
        countBadge.BackgroundColor3 = Color3.fromRGB(30, 70, 35)
    else
        countLbl.Text             = "👁  Thấy: 0 người"
        countBadge.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    end

    -- AIMBOT
    if not AIM_ENABLED then return end

    local center   = Vector2.new(vp.X / 2, vp.Y / 2)
    local fovPx    = getFOVpx()
    local nearPart = nil
    local nearDist = fovPx

    for _, player in ipairs(Players:GetPlayers()) do
        if not isEnemy(player) then continue end
        local char = player.Character
        if not char then continue end
        local part = char:FindFirstChild(CONFIG.AIM_TARGET_PART)
        if not part then continue end
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum or hum.Health <= 0 then continue end
        local sp, on = worldToVP(part.Position)
        if not on then continue end
        local d = (sp - center).Magnitude
        if d < nearDist then
            nearDist = d
            nearPart = part
        end
    end

    if nearPart then
        local cur = Camera.CFrame
        local tCF = CFrame.lookAt(cur.Position, nearPart.Position)
        Camera.CFrame = cur:Lerp(tCF, CONFIG.AIM_SMOOTH)
    end
end)

--==================================================
-- ESP BUTTON: bật → addESP, tắt → removeESP
--==================================================

espBtnRef.Activated:Connect(function()
    task.defer(function()
        if ESP_ENABLED then
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and isEnemy(p) then addESP(p) end
            end
        else
            for _, p in ipairs(Players:GetPlayers()) do removeESP(p) end
            espCount = 0
            countLbl.Text             = "👁  Thấy: 0 người"
            countBadge.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
        end
    end)
end)

--==================================================
-- COLLAPSE
--==================================================

collapseBtn.Activated:Connect(function()
    MENU_VISIBLE     = not MENU_VISIBLE
    content.Visible  = MENU_VISIBLE
    collapseBtn.Text = MENU_VISIBLE and "–" or "+"
    main.Size = MENU_VISIBLE
        and UDim2.fromOffset(MENU_W, MENU_H)
        or  UDim2.fromOffset(MENU_W, 46)
end)

--==================================================
-- PLAYER EVENTS
--==================================================

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function()
        task.wait(1)
        if ESP_ENABLED and isEnemy(player) then addESP(player) end
    end)
end)

Players.PlayerRemoving:Connect(removeESP)

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    for _, p in ipairs(Players:GetPlayers()) do removeESP(p) end
    if ESP_ENABLED then
        task.wait(0.5)
        for _, p in ipairs(Players:GetPlayers()) do
            if isEnemy(p) then addESP(p) end
        end
    end
end)

--==================================================
-- DRAG MENU
--==================================================

local dragging, dragStart, startPos = false, nil, nil

header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging  = true
        dragStart = input.Position
        startPos  = main.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if sliderDragging then return end   -- ưu tiên slider
    if not dragging then return end
    if input.UserInputType ~= Enum.UserInputType.MouseMovement
    and input.UserInputType ~= Enum.UserInputType.Touch then return end
    local d = input.Position - dragStart
    main.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + d.X,
        startPos.Y.Scale, startPos.Y.Offset + d.Y
    )
end)

--==================================================
print("[Chameleon ESP v5] Loaded! PlaceId: 110808833601416")
print("NEW: Badge ESP count | Lines từ TOP | Auto-ESP đổi team | FOV % slider")
